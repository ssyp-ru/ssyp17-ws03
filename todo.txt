
resourse_manager:
    1) XmlElem * - мы не используем простые указатели, исправить на shared_ptr
    2) слишком длинные строчки (120 символов - уже граница того, что не стоит пересекать)
    3) неудача загрузки спрайта - сообщение уровня Info , а не дебага
    4) Если get_subimage вернул nullptr, его не нужно фигачить в контейнер
    5) зачем ты создаешь переменную Animation, а потом из неё создаешь shared_ptr, не лучше сразу сделать умный указатель?



Image:
    1) проверки на правильные x и y в параметрах функции
    2) Если конструкция объекта провалилась (например нет файла), то функции также не должны выполняться
    3) написать комментарии. Например нужно написать о том, что мы не поддерживаем картинки в режиме 1 байта (черно белые)

Animation:
    1) комментарии чуть меньше, чем бесполезны
    2) не нужно писать выражения типа if (is_smth) do_smth
    то есть на той же строчке, это неудобно

xml_parser:
    1) Куча указателей, нужно поменять на умные
    2) parse_xml тоже должна возвращать указатель (чтобы не копировать дынные)

graphic.h:
    1) нужны комментарии
    2) возможно некоторые функции уже не нужны?
    3) почему разные стили написания названий функций?

physic_core:
    1) комментарии пишутся не на строчке с функцией, а перед ней
    2) комментарии - это хорошо. Если они нужны. Например такой в целом бесполезен - 
    Vector2f getPosition(); // Returns current gameobject position
    3) У тебя сравнение векторов неправильное (смотри math.cpp)

logger:
    1) 1000000 - магическая константа
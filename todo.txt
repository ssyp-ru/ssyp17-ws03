// Arthurii - пометил '?' пункты, которые я вроде как сделал.
?1) Сейчас код, который находится в мастере, не компилируется. Это очень, очень, очень плохо.
xml_parser + лог:
2) shared_ptr?
?3) Зачем точность в наносекундах? 
?4) По-хорошему функции, связанные с замерением времени, не должны быть в классе log. Нужно выделить в отдельный класс TimeUtil
5) вместо 
    if ( expression ){
        куча кода
    } else {
        одна строчка
    }
    return
лучше писать
    if ( !expression ){
        return
    }
    куча кода
?6) строчка, размером 192 столбца....  Это как-то не
7) названия переменных. Вот, например, переменная rm. Что это такое?
8) WTF? 0_o
    if(line[i] == '\t') break; // hate tabs 
9) В коде парсера очень нужны комментарии
10) А также тесты (за консультацией об оформлении тестов можно обратиться к Васе)
11) почему функция track, если она "starts track" не называется start_track?
12) output - плохое название переменной

windows:
1) Выход из программы по exit() - очень плохо. Нужно расширить интерфейс окна функцией, которая будет завершать работу приложения.
2) По поводу изменения интерфейса - обсудим

Кода остальных участников я в гите не увидел
Когда я проснусь, я расскажу про cmake и напомню схему разработки нашего проекта. 
ps. Напомните мне рассказать про range-based for 